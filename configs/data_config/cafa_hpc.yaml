# paths and parameters for cafa data
dataset_root: /orcd/home/002/lerchen/code/cafa_proj/cafa6
train_dir: ${.dataset_root}/Train_slice

sources:
  seqs_path: ${..train_dir}/train_sequences.fasta
  terms_path: ${..train_dir}/train_terms.tsv
  ia_path: ${..dataset_root}/IA.tsv
  go_path: ${..train_dir}/go-basic.obo

split:
  seed: 1337
  ratios:
    train: 0.8
    val: 0.1
    test: 0.1

train_csv: null
val_csv: null
test_csv: null
embeddings_npz: null
go_prior_npz: null

go_prior:
  obo_path: ${..sources.go_path}
  train_split_csv: null
  top_k:
    MF: 528
    BP: 1024
    CC: 528

neighbors:
  batch_size: 64
  top_k: 20
  cosine_cutoff: 0.3
  max_fanout_1: 2
  max_fanout_2: 1
  global_train_path: null
  global_val_path: null
  global_test_path: null
  
min_length: 10
train_manifest: null
val_manifest: null
test_manifest: null
